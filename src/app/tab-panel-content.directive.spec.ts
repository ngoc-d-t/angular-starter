import { TabPanelContentDirective } from './tab-panel-content.directive';

describe('TabPanelContentDirective', () => {
  it('should create an instance', () => {
    const directive = new TabPanelContentDirective();
    expect(directive).toBeTruthy();
  });
});
